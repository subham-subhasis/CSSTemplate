import { RootDirective } from './root.directive';

describe('RootDirective', () => {
  it('should create an instance', () => {
    const directive = new RootDirective();
    expect(directive).toBeTruthy();
  });
});
