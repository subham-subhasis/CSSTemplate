import { StopPropgationDirective } from './stop-propgation.directive';

describe('StopPropgationDirective', () => {
  it('should create an instance', () => {
    const directive = new StopPropgationDirective();
    expect(directive).toBeTruthy();
  });
});
